#!/usr/bin/env sh
# FAST VERSION: Only lint and typecheck, skip tests
# To use: cp .husky/pre-commit.fast .husky/pre-commit

. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for file-specific checks
npx lint-staged

# Run full typecheck (fast, catches global type errors)
npm run typecheck
